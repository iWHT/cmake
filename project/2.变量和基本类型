2.1.1
1. 类型int、long、long long和short的区别是什么？无符号类型和带符号类型的区别是什么？float和double的区别是什么？
在C++中，不同整数类型主要区别在于它们的大小（即可以存储的整数范围）以及它们是否可以表示负值。

**1. 基本整数类型的区别:**

- **`int`**: 这是最常见的整数类型，大小至少为16位，但通常在现代系统上是32位。它可以是带符号的（默认），表示它可以存储负数、零和正数。

- **`long`**: 在32位系统上，`long`经常和`int`有相同的大小，都是32位的；而在64位系统上，`long`往往是64位。它同样可以是带符号的。

- **`long long`**: 这是一个更大的整数类型，保证至少为64位。它能够存储比`int`或`long`更大或更小的数值，同样默认是带符号的。

- **`short`**: 这是一个较小的整数类型，至少为16位。适用于数值范围不大时候以节省空间。

**2. 无符号类型和带符号类型的区别:**

- **带符号类型（`signed`）**: 可以表示正数、负数以及零。例如，带符号的`int`可以从负数范围开始，经过零，到达正数范围。

- **无符号类型（`unsigned`）**: 只能表示非负数，包括零和正数。因此，它们的数值范围始于零，达到带符号类型最大正数的两倍。例如，`unsigned int`可以存储的最大值是`signed int`所能存储最大值的两倍。

使用无符号类型的好处是可以扩大可表示的正数范围，但要牢记它们不能表示负值。

**3. `float` 和 `double`的区别:**

- **`float`**: 是一种单精度浮点类型，通常为32位。它提供大约6到7位十进制精度，通常足够用于一般的浮点运算。

- **`double`**: 是一种双精度浮点类型，通常为64位。它提供大约15到16位十进制精度，对于需要更高精度和更广范围的数值运算更为合适。

简而言之，`double`类型在精度方面要优于`float`，因此在需要非常精确的浮点运算时，通常会选择使用`double`。

在选择使用哪种类型时，要考虑数据的大小和精度要求以及程序对内存效率的要求。例如，在嵌入式系统或者对性能有极致要求的应用中，可能会更多地考虑使用`short`或`unsigned`类型来节省内存或提高计算速度。而在进行科学计算或者需要非常高精度的数值运算时，可能会优先考虑`long long`或`double`。

2. 计算按揭贷款时，对于利率、本金和付款分别应选择何种数据类型? 
在计算按揭贷款时，你需要选择能够适当表示货币值和利率精度的数据类型。通常对于金融相关计算，推荐使用足够精确的浮点数类型。在C++中，你可以使用以下数据类型：

- **本金（Principle Amount）**：本金是贷款的原始总额，通常是一个较大的数值，但不需要非常高的精度。因为涉及到货币，通常本金不会有小数部分，或者最多有两位小数（比如表示美元和美分）。在这种情况下，你可以使用`double`类型或更精确的`long double`。使用整数类型（例如`long long`）也是可能的，特别是当你以较小的货币单位（如美分）存储值时。

- **利率（Interest Rate）**：利率通常是一个小数，需要一定的精度来准确计算出利息。利率通常在百分比形式下小于100%，并有多位小数。因此使用`float`能够提供足够的精度，但更常用的是`double`，为了获得更高的精度和减少累积误差。

- **付款（Payment）**：付款金额可以是本金加上利息的总和，因此付款同样需要足够的精度。通常情况下，使用`double`类型可以提供足够的精度，同时也适合进行后续的金融计算。

在金融应用中，还有一点非常重要，那就是避免浮点数运算可能带来的舍入误差。在某些情况下，为了保证货币计算的精确度，开发者可能会使用整数类型来表示货币的最小单位（如以美分代替美元来存储数值），或者使用特殊的库（比如C++的`<decimal>`库或第三方财务计算库）来处理货币数值，以确保计算不会丢失精度。

总结一下，对于按揭贷款计算：
- 本金可以使用`double`或对应货币最小单位的整数类型
- 利率可以使用`double`，因为它提供额外的精度
- 付款金额可以使用`double`，以便与利率进行精确计算

请记住，在现实世界的金融领域中，准确性非常重要，选择合适的数据类型和计算方法以避免舍入误差是至关重要的。

2.1.2
将 -42 转换为 unsigned int 的过程如下：
-42 对 2^32 取模得到的结果就是 2^32 - 42，因为模运算的定义是找到一个非负整数，使得被除数能够表示成除数乘以整数加上这个非负整数的形式。
所以计算过程如下：
-42 + 2^32 = 4294967254
1. 读程序写结果。
unsigned u = 10, u2 = 42;
std::cout << u2 - u << std::endl;
32
std::cout << u - u2 << std::endl;
4294967296 - 32 = 4294967264

int i = 10, i2 = 42;
std::cout << i2 - i << std::endl;
32
std::cout << i - i2 << std::endl;
-32
std::cout << i - u << std::endl;
0
std::cout << u - i << std::endl;
0

2.1.3
1. 指出下述字面值的数据类型并说明每 一组内几种字面值的区别:
(a) 'a', L'a', "a", L"a"
(b) 10, 10u, 10L, 10ul, 012, OxC
(C) 3.14, 3.14f, 3.14L
(d) 10, 10u, 10., 10e-2
让我们依次看一下每组字面值的数据类型以及它们之间的区别：

### (a) 字符和字符串字面值
- `'a'`: 这是一个字符字面值，数据类型是 `char`。
- `L'a'`: 这是一个宽字符字面值，数据类型是 `wchar_t`。它用于表示可以超出普通 `char` 类型范围的字符集，比如 Unicode 字符。
- `"a"`: 这是一个字符串字面值，数据类型是 `const char[2]`，其中包含了字符 'a' 和一个空字符 `\0` 来表示字符串的结束。
- `L"a"`: 这是一个宽字符串字面值，数据类型是 `const wchar_t[2]`，同样包含了字符 'a' 和一个空字符，但这里是宽字符的空字符。

### (b) 整数字面值
- `10`: 这是一个十进制字面值，数据类型是 `int`。
- `10u`: 这是一个十进制无符号字面值，数据类型是 `unsigned int`。
- `10L`: 这是一个长整型十进制字面值，数据类型是 `long int`。
- `10ul`: 这是一个无符号长整型十进制字面值，数据类型是 `unsigned long int`。
- `012`: 这是一个八进制字面值（因为它以 0 开头），数据类型是 `int`，其十进制值为 10。
- `0xC`: 这是一个十六进制字面值（因为它以 `0x` 开头），数据类型是 `int`，其十进制值为 12。

### (c) 浮点数字面值
- `3.14`: 这是一个双精度浮点数字面值，数据类型是 `double`。
- `3.14f`: 这是一个单精度浮点数字面值，数据类型是 `float`。
- `3.14L`: 这是一个扩展精度浮点数字面值，数据类型是 `long double`。

### (d) 混合字面值
- `10`: 这是一个十进制字面值，数据类型是 `int`。
- `10u`: 这是一个十进制无符号字面值，数据类型是 `unsigned int`。
- `10.`: 这是一个双精度浮点数字面值，数据类型是 `double`，这里的点表示这是一个浮点数。
- `10e-2`: 这是一个指数表示法的双精度浮点数字面值，数据类型是 `double`，其值相当于 10 乘以 10 的负二次方，即 0.1。

注意：每当字面值中包含小数点或指数部分时，它会被认为是一个浮点数字面值，除非显式地标记为 `f` 或 `F` 作为 `float` 类型，或者 `l` 或 `L` 作为 `long double` 类型。无标记的整数字面值默认为 `int` 类型，但如果后跟 `u` 或 `U` 则为 `unsigned int`，后跟 `l` 或 `L` 则为 `long int`，后跟 `ul`、`UL`、`lu` 或 `LU` 则为 `unsigned long int`。字面值前的 `0` 表示八进制，前缀 `0x` 或 `0X` 表示十六进制。

2. 下面两组定义是否有区别，如果有，请叙述之:
int month = 9, day = 7;
int month = 09, day = 07:
是的，这两组定义有重要的区别，尤其是针对数值的表示方法。
对于第一组：
int month = 9, day = 7;
这里的 `month` 和 `day` 都是使用十进制字面值初始化的普通整型（`int`）变量。`month` 的值是 9，`day` 的值是 7。
对于第二组：
int month = 09, day = 07;
这里的问题在于 `month` 的初始化。`09` 是一个以 `0` 开头的整数字面值，意味着它应被解释为八进制（base-8）字面值，但八进制数字只能包括 0-7 的数字，所以 `09` 不是一个有效的八进制数。因此，这个定义会导致编译错误。

对于 `day = 07;`，`07` 是一个有效的八进制字面值，等价于十进制的 7。在八进制中，数字 7 表示的值和在十进制中一样，所以即使是八进制表示法，它的值依然是 7。
因此，第一组定义是正确的，`month` 和 `day` 被分别初始化为 9 和 7。第二组定义中，`day` 初始化为 7 没有问题，但是试图将 `09` 赋值给 `month` 会导致编译错误。

3. 下述字面值表示何种含义?它们各自的数据类型是什么?
(a) "Who goes with F\145rgus?\012"
(b) 3.14e1L 
(c) 1024f
(d) 3.14L

### (a) 字符串字面值
"Who goes with F\145rgus?\012"
- 含义：这是一个包含转义序列的字符串字面值。`\145` 表示八进制转义序列，对应于十进制的 101，它是 ASCII 表中小写字母 'e' 的编码。`\012` 代表八进制转义序列，对应于十进制的 10，它是 ASCII 表中换行符（Line Feed, LF）的编码。
- 数据类型：`const char[]`
字符串实际的内容等同于 "Who goes with Fergus?\n" — 其中 `\n` 表示换行。

### (b) 浮点字面值
3.14e1L
- 含义：这是一个带有指数部分的浮点字面值，`e1` 表示乘以 10 的 1 次方，所以这个字面值等于 3.14 * 10^1，即 31.4。
- 数据类型：`long double`

### (c) 无效的浮点字面值
1024f
- 含义：这看起来像是想表示一个 `float` 类型的浮点数，但是整数字面值后面不能直接跟随 `f`。这会导致编译错误，因为 `1024` 是一个整数，所以后面不能加 `f`。如果要表示浮点数，应该是 `1024.0f`。
- 数据类型：编译错误，如果意图表示 `float`，应写作 `1024.0f`。

### (d) 浮点字面值
3.14L
- 含义：这是一个 `long double` 类型的浮点字面值。
- 数据类型：`long double`

在C++中，可以通过后缀来指定浮点字面值的类型：无后缀指定 `double`，`f` 或 `F` 指定 `float`，`l` 或 `L` 指定 `long double`。由于 (c) 中的表达式尝试将 `float` 后缀应用于一个整数，这是不允许的，正确的 `float` 字面值应使用小数点：例如 `1024.0f`。

4. 请利用转义序列编写一段程序，要求先输出2M，然后转到新一行。修改程序 使其先输出2 ，然后输出制表符，再输出M，最后转到新一行。
std::cout << "2\115\n";
std::cout << "2\t\115\n";

2.2.1
1. 解释下列定义的含义。对于非法的定义，请说明错在何处并将其改正。
(a) std::cin >> int input_value; 
(b) int i = { 3.14 }; 
(c) double salary = wage = 9999.99; 
(d) int i = 3.14;

### (a) 非法的输入语句
std::cin >> int input_value;
- 含义：尝试从标准输入流（`std::cin`）读取一个值到变量 `input_value` 中。
- 错误：类型声明（`int`）不能出现在输入操作符（`>>`）的右侧。应先声明变量，然后才能将输入读入到该变量中。
- 更正：
    int input_value;
    std::cin >> input_value;

### (b) 类型不匹配的列表初始化
int i = { 3.14 };
- 含义：使用列表初始化（用花括号 `{}` 包裹的值）来声明并初始化一个整数变量 `i`。
- 错误：列表初始化不允许缩窄转换。`3.14` 是一个 `double` 值，将其赋值给 `int` 类型会丢失小数部分，所以列表初始化会引发错误。
- 更正：
    int i = { 3 }; // 或者直接使用 int i = 3; （去掉花括号）

### (c) 未声明的变量
double salary = wage = 9999.99;
- 含义：尝试进行链式赋值，将变量 `wage` 的值赋给变量 `salary`。
- 错误：`wage` 未声明。在这个表达式中，`salary` 和 `wage` 都必须是已声明的变量，否则就是非法定义。
- 更正：首先声明 `wage`，然后再赋值给 `salary`。
    double wage = 9999.99;
    double salary = wage;
    如果意图是同时声明 `salary` 和 `wage` 并初始化为同一个值，则应该分别声明：
    double wage = 9999.99;
    double salary = 9999.99;

### (d) 精度损失的隐式转换
int i = 3.14;
- 含义：声明一个整型变量 `i` 并尝试用 `double` 类型的字面值 `3.14` 初始化它。
- 错误：这是合法的 C++ 代码，但是赋值时会发生隐式类型转换，会使 `double` 类型的值 `3.14` 被转换（截断）成 `int` 类型的值 `3`，这意味着小数部分 `0.14` 会丢失。
- 更正：如果想保留小数部分，应该使用 `double` 类型的变量：
    double i = 3.14;
    如果只需整数部分，上述定义是有效的，但应记住小数部分将被舍弃。

2. 下列变量的初值分别是什么?
std:string global_str; //空字符串
int global_int; //0
int main()
{
    int local_int; //未定义
    std::string local_str; //空字符串
}

2.3.1 
1. 下面的哪个定义是不合法的?为什么?
(a) int ival = 1.01; // 合法的定义，但可能导致精度丢失
(b) int &rval1 = 1.01; // 不合法的定义，因为 1.01 是一个 double 类型的字面值，你不能将一个非常量引用绑定到一个字面值上。常量引用可以绑定到字面值，但非常量引用不行。
(c) int &rval2 = ival; // 合法的定义
(d) int &rval3; // 不合法的引用定义，因为引用在创建时必须被初始化绑定到一个合适的对象上。
2. 考查下面的所有赋值然后回答:哪些赋值是不合法的?为什么?哪些赋值是 合法的?它们执行了什么样的操作?
int i = 0, &r1 = i;
double d = 0, &r2 = d;
(a) r2 = 3.14159; //合法的赋值
(b) r2 = r1; //合法的赋值但有隐式类型转换
(c) i = r2; //合法的赋值但有精度损失
(d) r1 = d; //合法的赋值但有精度损失

2.4.2
1. 下面的哪些初始化是合法的?请说明原因。
(a) int i = -1, &r = 0; //不合法,非常量引用 r 不能绑定到字面值。只有常量引用可以绑定到字面值。
(b) int *const p2 = &i2; //合法
(c) const int i= -1, &r=0; //合法，常量引用可以绑定到字面值。
(d) const int *const p3 = &i2; //合法
(e) const int *p1 = &i2; //合法
(f) conts int &const r2; //不合法，引用本身不可以是const
(g) const int i2 = i, &r = i; //合法
2. 说明下面的这些定义是什么意思，挑出其中不合法的。
(a) int i, *const cp; //不合法，cp 没有在定义时被初始化。常量指针必须在定义时初始化，因为之后无法修改它指向的地址。
(b) int *p1, *const p2; //不合法，常量指针 p2 没有在定义时被初始化。
(c) const int ic, &r = ic;//不合法，常量 ic 没有在定义时被初始化。常量在创建时必须初始化，因为它的值之后不能改变。
(d) const int *const p3;//不合法，p3 没有在定义时被初始化。作为常量指针，p3 必须在声明的时候被初始化。
(e) const int *p; //合法
